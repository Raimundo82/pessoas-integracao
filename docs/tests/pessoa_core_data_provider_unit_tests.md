# PessoaCoreDataProvider Unit Test Cases

| #   | Input Import Keys (`PessoaImportKey[]`)                                                      | PersonalDataClient Behavior (`GetPersonalDataAsync`)               | Translator Behavior (`Translate`)          | Expected Output (`Dictionary<PessoaImportKey, PessoaCoreDataFragment>`) | Test Name (`Should...When...`)                                       |
| --- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------ | ----------------------------------------------------------------------- | -------------------------------------------------------------------- |
| 1   | `[ { Nii = "22600", ExternalId = "30002697" }, { Nii = "22700", ExternalId = "30002797" } ]` | Returns empty dictionary                                           | Not called                                 | Client called once with exact import keys                               | `ShouldCallPersonalDataClientWithSameImportKeys_WhenGettingCoreData` |
| 2   | `[]`                                                                                         | Returns empty dictionary; captures cancellation token              | Not called                                 | Same token is forwarded to client                                       | `ShouldForwardCancellationToken_WhenGettingCoreData`                 |
| 3   | `[ { Nii = "22600", ExternalId = "30002697" }, { Nii = "22700", ExternalId = "30002797" } ]` | Returns dictionary with 2 entries (each with `ZhrSPessoaisOutput`) | Called once per entry with matching output | Translator invoked exactly once per entry with correct output value     | `ShouldCallTranslatorForEachEntry_WhenClientReturnsMultipleResults`  |
| 4   | `[ { Nii = "22600", ExternalId = "30002697" }, { Nii = "22700", ExternalId = "30002797" } ]` | Returns dictionary with 2 entries                                  | Returns `DadosPessoais` for each           | Result dictionary contains same keys as client output                   | `ShouldReturnDictionaryWithSameKeys_WhenClientReturnsResults`        |
| 5   | `[ { Nii = "22600", ExternalId = "30002697" } ]`                                             | Returns dictionary with 1 entry                                    | Returns specific `DadosPessoais` instance  | Fragment wraps the exact `DadosPessoais` object returned by translator  | `ShouldWrapTranslatedDadosPessoaisInFragment_WhenBuildingResult`     |
| 6   | `[]`                                                                                         | Returns empty dictionary                                           | Never called                               | Empty dictionary; translator not invoked                                | `ShouldReturnEmptyDictionary_WhenClientReturnsEmptyDictionary`       |
| 7   | `[]`                                                                                         | Throws `InvalidOperationException("SOAP error")`                   | Not reached                                | Exception propagated to caller                                          | `ShouldThrowException_WhenPersonalDataClientThrows`                  |
